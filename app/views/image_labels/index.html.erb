<p id="notice"><%= notice %></p>

<h1>Image Labels</h1>

<table>
  <thead>
    <tr>
      <th>Image</th>
      <th>Label</th>
      <th>Confidence</th>
      <th>Source</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @image_labels.each do |image_label| %>
      <tr>
        <td><%= image_label.image_id %></td>
        <td><%= image_label.label_id %></td>
        <td><%= image_label.confidence %></td>
        <td><%= image_label.source %></td>
        <td><%= link_to 'Show', image_label %></td>
        <td><%= link_to 'Edit', edit_image_label_path(image_label) %></td>
        <td><%= link_to 'Destroy', image_label, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Image Label', new_image_label_path %>
